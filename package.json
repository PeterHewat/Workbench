{
  "name": "workbench",
  "version": "0.1.0",
  "description": "Workbench — small, self-contained browser tools",
  "author": "Peter Hewat",
  "homepage": "https://peterhewat.github.io/Workbench/",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PeterHewat/Workbench.git"
  },
  "license": "MIT",
  "private": true,
  "packageManager": "bun@1.4.2",
  "engines": {
    "bun": ">=1.4.2",
    "node": ">=26.0.0"
  },
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "bun run --filter @workbench/vellum dev",
    "build": "bun tools/build-site.ts",
    "preview": "bun run --filter @workbench/home preview",
    "lint": "eslint .",
    "format": "prettier --check .",
    "format:fix": "prettier --write .",
    "typecheck": "bun run --filter '*' typecheck",
    "check": "bun run lint && bun run typecheck && bun run format",
    "test": "bun run --filter '*' test",
    "verify": "bun run check && bun run test && bun run build",
    "new-app": "bun tools/new-app.ts",
    "outdated": "bun outdated -r",
    "update": "bun update -r",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.{ts,tsx,js,mjs,cjs}": [
      "eslint --fix",
      "prettier --write"
    ],
    "*.{json,md,yml,yaml,css,html}": "prettier --write"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "@types/bun": "^1.4.2",
    "eslint": "^10.10.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-prettier": "^5.5.6",
    "globals": "^17.12.0",
    "husky": "^9.1.7",
    "lint-staged": "^17.5.1",
    "prettier": "^3.9.7",
    "typescript": "~6.0.3",
    "typescript-eslint": "^8.70.0",
    "vite": "^8.3.0"
  }
}
